import React from "react";

import icon from '../assets/google.png'
import SignupProps from "../Reusables/SignupProps";

const Signin = () => {
    return (
        <SignupProps image={icon} content="ggg"/>
     );
}
 
export default Signin;
const SignupProps = styled.button`

`
const SignupProps = ({ Icon, primaryText, secondaryText }) => {
  return (
    <SignupContainer>
      <Button>
        <Icon src={GoogleIcon} alt="" /><Icon/>
        <Text>{primaryText="Signup with mail"}</Text>
      </Button>
      <SecondaryText>{secondaryText="sign in with mail"}</SecondaryText>
    </SignupContainer>
  );
};

export default SignupProps;


const SignupContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
`;

const Button = styled.button`
  display: flex;
  align-items: center;
  background-color: #4285f4;
  border: none;
  color: white;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 16px;

  &:hover {
    background-color: #357ae8;
  }
`;

const Icon = styled.img`
  width: 20px;
  height: 20px;
  margin-right: 10px;
`;

const Text = styled.span`
  font-weight: bold;
`;

const SecondaryText = styled.p`
  font-size: 14px;
  color: #666;
`;